# Benchmark
- This benchmark is designed to work with [poop](https://github.com/andrewrk/poop).

# How to run?
```bash
zig build bench-<folder + filename>
```
- For example, some available benchmarks: `ecs-query`, `ecs-stress_query`, ...
